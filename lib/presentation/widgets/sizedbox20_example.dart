import 'package:flutter/widgets.dart';

class SizedBox20Example extends StatelessWidget {
  const SizedBox20Example({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
      width: 20,
    );
  }
}